def calcul(x, y):
    if x>y:
        return 0
    elif x==y:
        return 1
    else:
        return calcul(x+1, y)+calcul(x*2, y)
print(calcul(3, 14)*calcul(14, 62) - calcul(3,14)*calcul(14,59)*calcul(59,62))
