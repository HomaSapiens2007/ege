def calcul(x, y):
    if x > y:
        return 0
    if x==y:
        return 1
    if x<y:
        return calcul(x+1, y) + calcul(x+2,y) + calcul(x*3, y)
print(calcul(2,9)*calcul(9,11)*calcul(11,12))
